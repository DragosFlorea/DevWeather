<Page
    x:Class="DevWeather.AddNewLoc_Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DevWeather"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:model="using:DevWeather.ViewModels"
    DataContext="{Binding ListPageInstance, Source={StaticResource Locator}}">
    <Grid Background="{ThemeResource ApplicationPointerOverForegroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
        </Grid.RowDefinitions>
        <ListBox Grid.Row="1" x:Name="MyListbox" ItemsSource="{Binding ListWeatherData}">
            <ListBox.ItemTemplate >
                <DataTemplate x:DataType="model:WeatherData_VM">
                    <Grid Width="700" Background="HotPink">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Row="0" Name="TempTxtblk" FontSize="35" Text="{x:Bind ReqWeather.main.temp}"></TextBlock>
                        <TextBlock Grid.Row="1" Name="PressTxtblk" FontSize="35" Text="{x:Bind ReqWeather.main.pressure}"></TextBlock>
                        <TextBlock Grid.Row="2" Name="LocationTxtblk" FontSize="35" Text="{x:Bind Reqlocation}"></TextBlock>
                        <Image Grid.Column="1" Grid.Row="0" Name="ImageSrc" Source="{x:Bind ReqIcon}" Width="150" Height="100"></Image>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBoxItem>

            </ListBoxItem>
        </ListBox>
        <StackPanel Grid.Row="3">
        <ToggleSwitch x:Name="UnitsToggle" Header="C/F" OffContent="C" OnContent="F" Foreground="Aqua" HorizontalAlignment="Left" Toggled="UnitsToggle_Toggled"/>
            <Button Width="50" Height="50" Background="AliceBlue" HorizontalAlignment="Right" VerticalAlignment="Top" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Page>
